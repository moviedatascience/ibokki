# Caddy reverse proxy for ibokki.com — automatic Let's Encrypt TLS.
# Install caddy (https://caddyserver.com/docs/install), drop this at
# /etc/caddy/Caddyfile, then: systemctl reload caddy
ibokki.com, www.ibokki.com {
	encode gzip
	reverse_proxy 127.0.0.1:7788
}
